{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "history", "watchlist", "timeshare"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-open"
  ]
}
